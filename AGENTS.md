<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Project & Code Guidelines

1. **Language:** Always communicate, document, and write responses strictly in English.
2. **No Emojis & Brand Icons Invariant:** Do not use emojis in UI components, logs, code, or documentation unless explicitly requested by the user. Use clean Lucide SVG icons for interface iconography. Because modern `lucide-react` packages do not export third-party brand logos (e.g. GitHub, Twitter, LinkedIn), never import `Github` from `lucide-react`; use an established inline SVG component (`GithubIcon`) instead.
3. **File Size Limit:** Keep all modular files (frontend components, hooks, backend route handlers, and utilities) under 200 lines of code.
4. **Tone & Profile Alignment:** The user is a marketing professional with a casual, authentic, and approachable voice who bridges commercial strategy with practical technology, data analytics, and automated workflows. He is not a software engineer (open to becoming one in the future, but currently a marketer who knows and applies practical tech); avoid exaggerated engineering titles, corporate buzzwords, or pretending to be an enterprise developer. Position his technical and coding capabilities as practical enablers for better marketing, operational workflows, and business growth.
5. **Multi-Language (i18n) Content Synchronization Rule:** Whenever any content, text, project, metric, case study, or UI label is added or updated in the portfolio, you MUST update BOTH English and Spanish versions (`src/data/i18n/siteData.en.json` & `src/data/i18n/siteData.es.json`, `src/data/i18n/ui.en.ts` & `src/data/i18n/ui.es.ts`, and `src/data/i18n/caseStudies.ts`). You MUST run `npm run test:run` to verify that the automated parity test (`src/data/i18n/__tests__/i18nSync.test.ts`) passes with 100% key and structure symmetry before concluding.
6. **Core Brand Credentials & Editorial Showcase Invariant:**
    - **Credential Accumulator Anti-Pattern:** Never display aggregate credential counts (e.g. "103 certificates"); raw totals project an amateur course-collector image rather than senior practitioner authority. Always frame credentials qualitatively by accredited issuer authority (Google, Microsoft, HubSpot, CertiProf) and practical marketing/analytics application.
    - **Monumental Two-Line Typography:** The section title must render as a monumental `<h1>` with multi-line configuration (`lines={[titleLine1, titleLine2]}`), with every word wrapped in `inline-block whitespace-nowrap` to prevent mid-word hyphenation or awkward line breaks.
    - **Editorial Partner Row & Icon-Free CTA:** Underneath the concise editorial subtitle, display a clean horizontal row of verified accredited issuers/keywords (Google, Microsoft, HubSpot, CertiProf, Python Institute). The global archive link ("Acceder a todas las credenciales" / "View All Verified Credentials") must be a clean, high-contrast typographic pill button strictly free of icons or emojis.
7. **Color Palette, Borderless Optical Blur & Visual Restraint:** When styling accents, CTAs, and active states, use subdued, architectural slate blues (`#21426E` light / `#4876AB` dark) rather than saturated electric cobalt (`#1E40AF`) or neon colors. The top navigation header must use a completely colorless, borderless optical blur gradient (`bg-transparent backdrop-blur-xl`) masked with a smooth downward linear feather (`[mask-image:linear-gradient(to_bottom,black_0%,black_35%,transparent_100%)]`), completely eliminating milky white/grey background fills (`bg-bg-glass`), hard bounding-box cutoffs, and horizontal border lines across both light and colored backgrounds.
8. **Architectural Indexing & Monospace Precision:** Major page sections must maintain systematic bracketed index tags (`[01]`, `[02]`, etc.) via `SectionLabel`, and numeric progress or item counters must use high-precision monospace fractional formatting (`01 / 05`).
9. **Navigation Minimalism & Action Pairing:** Keep the top header navigation ultra-clean. Primary contact actions must avoid redundant contact forms, offering strictly two high-intent options: direct instant messaging (**WhatsApp**) and conversational booking (**Schedule Call** dispatched via custom event `open-clippo-schedule` to the Clippo assistant). Secondary site preferences (theme toggling, language switching) are delegated entirely to Clippo.
10. **Hero Centerpiece & Environmental Workspace Portraits:** When workspace/desk setup photography is used in the hero centerpiece, preserve the environment using a wide responsive frame (`aspect-[16/10]` up to `max-w-2xl`) rather than force-cropping to a rigid square. Keep the hero composition editorial and uncluttered by avoiding redundant status eyebrows ("Available for Work", location pills) above the brand anchor.
11. **Kinetic Typography & Scroll-Scrubbed Text Reveals:** When implementing hero or major section title convergence effects (e.g. `ScatterTitle`), use pure Framer Motion with deterministic pseudo-random character seeding (`pseudoRandom(seed)`) to ensure SSR hydration safety. Text reveals must scrub character blur, rotation, and translation into sharp focus over scroll progress without external animation dependencies.
12. **About Me Section & Pinned Narrative Scrollytelling Invariant:** The second major section of the portfolio must be `[01] About me` (`#about`), immediately following the Hero. The hero subtitle remains concise (`profile.tagline`), while the in-depth profile narrative and sound money/systems telemetry are housed exclusively in `AboutMeSection`:
    - **Calibrated Pinned Stage ("Leve Pausa"):** The section is anchored as a pinned scroll-stage (`sticky top-0 h-[100dvh]` inside `h-[220vh]`, providing ~120vh of travel) where the editorial narrative, live telemetry bar, and contact actions remain centered and stationary in the viewport during scroll.
    - **Sequential Scroll-Scrubbed Text Illumination:** The 4 narrative paragraphs employ `ScrollIlluminatedParagraph` driven by the outer container's `scrollYProgress` across overlapping sequential ranges (`[0.06, 0.27]`, `[0.24, 0.46]`, `[0.43, 0.64]`, `[0.61, 0.83]`).
    - **Balanced Clause Segmentation & Wavefront Halo:** Paragraphs are automatically segmented into balanced clauses (~40–80 characters) preserving proper nouns and parenthetical lists. Text transitions from dormant muted slate (`opacity: 0.32`, `--color-text-muted`) through an active luminous accent halo (`textShadow: 0 0 12px rgba(147, 197, 253, 0.45)`) into crisp full contrast (`opacity: 1.0`, `--color-text-primary`).
    - **Telemetry & Action Pairing Convergence:** The borderless live telemetry bar (`BtcTrendTelemetryBar`) and dual action buttons (`Calendar` / `WhatsApp`) smoothly ramp to 100% opacity (`0.58 -> 0.80`) alongside the narrative resolution, settling in full clarity before the stage releases into `[02] ProjectsSection`.
    - **Pinned Container Coordinate Rule:** Child animations inside a `sticky top-0` stage must NEVER use local viewport intersection triggers (`useScroll({ target: childRef })`), as children remain physically stationary in the viewport while pinned. All child scrubbing MUST receive the outer container's `scrollYProgress` mapped through functional curves (`useTransform(scrollYProgress, (s) => ...)` per Rule 21).
13. **Slogan Architectural Transition & Unified Projects Header Invariant:** The brand slogan "Think Big. Start Small." is integrated directly as the monumental kinetic transition opening of `[02] ProjectsSection` (`#projects`), positioned between `AboutMeSection` and the projects showcase. It implements an extended pinned `320vh` scroll stage (`sticky top-0 h-[100dvh]` inside `h-[320vh]`, providing `220vh` of scroll travel) where the monumental typography, 4 quantifiable marketing KPI cards, and project headings remain anchored and steady in the center ("quieto") at 100% solid opacity across the first 92% of scroll (`useTransform(scrollYProgress, [0, 0.92, 1], [1, 1, 0])`), affording comfortable reading time before easing out into the showcase. Kinetic architectural background lines (`SloganBackgroundLines`: horizontal latitudes, vertical telemetry tracks, crosshairs, and diagonal beams) glide across the button-blue canvas (`#21426E` light / `#162C4E` dark) in multi-directional parallax. Eliminates standalone toolkit pill bars (`TrustedStack`), transitioning directly from the projects showcase into downstream sections.
14. **Projects Showcase & Accordion Scrollytelling Architectural Invariant:** The featured projects showcase exclusively employs the cinematic physical vertical accordion scroll-reveal architecture (inspired by the continuous list mechanics of `alejandroha.com/en`) without view toggles or redundant grid modes:
    - **Unified Full-Bleed Solid Blue Canvas:** The entire section must span full viewport width (100vw) in solid button blue (`#21426E` light / `#162C4E` dark), eliminating any outer margins or floating container silhouettes (`rounded-3xl` card on grey canvas). Section labels must render with the `variant="onDark"` high-contrast white token set.
    - **Full-Bleed Header Bar & Pure White Typography:** Full-bleed header bars with thin borders (`border-t border-b border-white/20 bg-black/15`) featuring bracketed monospace indices (`[01]`, `[02]`) and project titles rendered in crisp, pure white (`style={{ color: "#FFFFFF" }}` / `!text-white font-black uppercase text-xl sm:text-3xl`).
    - **Continuous Vertical Accordion & Upward Header Push:** All projects render within a continuous vertical list inside the pinned 100dvh viewport container. The active visual collapses its height from full viewport availability down to zero (`height: calc(100dvh - barHeight) -> 0px`). Because the visual collapses in natural flow, the incoming project's header and real content ascend together from the bottom edge (`top: 100vh`) to meet the active header. At the docking threshold ($s \in [i + 0.85, i + 1.0]$), the list wrapper translates upward by 1 header height (`y: -barHeight`), physically pushing the active header off the top of the viewport and docking the incoming header at `top: 0` as the new pinned title.
    - **Single Media Invariant (No Rear Peeking Cards):** Each project card has exactly one unique media presentation (`active.videoSrc` or `active.imageSrc`); never render secondary layered or peeking cards behind it ("las imágenes de cada card es única, no hay imagen trasera").
    - **Synchronized Card Alignment & Directional Reveal Invariant:** All project cards share the identical widescreen 16:10 horizontal frame (`w-full aspect-[16/10] max-h-[calc(100dvh-6rem)]`) with matching left and right boundaries across the media track, eliminating awkward voids. During scroll transitions, cards appear and disappear strictly aligned ("tienen que aparecer y desaparecer alineados"): the incoming card unfolds from left to right (`clipPath: inset(0 right% 0 0)`), while the outgoing card disappears towards the right (`clipPath: inset(0 0 0 left%)`, `x: 0 -> 30px`, `opacity: 1 -> 0`). Never apply asymmetrical scaling (`scale: 0.62` to top-right) that pulls the card's boundary away from alignment.
    - **Continuous Video Looping & Viewport Lifecycle:** Project showcase videos must be configured with `autoPlay`, `loop`, `muted`, `playsInline`, `preload="auto"`, and an `onEnded` loop restart fallback. Playback lifecycle must be governed by an `IntersectionObserver` (threshold `0.02`) so the video loops continuously as long as it is visible on screen, pausing only when its accordion collapses to zero or leaves the viewport. Never pause videos prematurely on index state changes.
    - **Integrated Technology Tags & Conciseness:** Core technologies and toolkits must be integrated directly into project card tags (`active.tags`, rendering up to 5 tags per card) and detailed in `SkillsGrid` and `CertificationsSection`, eliminating separate standalone stack bars that needlessly elongate the website.
    - **Tailwind CSS v4 Cascade Layer Hygiene:** All global tag selectors (`html, body, h1..h6`) must be wrapped in `@layer base { ... }` in `globals.css` so that utility classes like `.text-white` are never overridden by unlayered tag rules in light mode.
    - **Modular Architectural Decomposition & Strict File Limits:** The scrollytelling engine is decomposed into modular subcomponents: `ProjectsShowcaseStage.tsx`, `ProjectAccordionItem.tsx`, `ShowcaseHeaderBar.tsx`, `ShowcaseNarrativePanel.tsx`, `ShowcaseMediaCard.tsx`, `SloganSection.tsx`, and `SloganBackgroundLines.tsx`. Every component file MUST remain strictly under 200 LOC per Rule 3.
15. **Hydration Immunity & Browser Extension Resilience:** To prevent SSR/client hydration mismatches, elements susceptible to third-party browser extensions (e.g., accessibility tools like UserWay injecting `data-uw-*`, `uw-v-*`, or `--uw-transform` attributes into `<video>` tags) must guard active dynamic media mounting with an `isMounted` state (`useState(false)` + `useEffect`). During SSR and initial hydration, render a static poster image (`<img>`); mount `<video>` or interactive players only after hydration, paired with `suppressHydrationWarning`. Kinetic text components performing mathematical character scattering or transform calculations (e.g., `ScatterTitle`) must render clean, semantic text during SSR and hydration for SEO and zero-delta DOM matching, activating character-level motion and dynamic transforms only post-hydration via `isMounted`.
16. **Honest Telemetry & Serverless State Contract Invariant:** Live telemetry endpoints (`/api/telemetry`) and UI consumers (`BtcTrendTelemetryBar`, `TelemetryStatusBadge`, `BtcPriceCard`) must NEVER return synthetic mock data or report false `"online"` statuses when upstream services are dormant, cold-starting, or failing. Telemetry pipelines must implement honest tripartite status mapping:
    - `live`: Upstream returns HTTP 200 with valid data.
    - `waking_up`: Upstream returns 502/503/504 or exceeds timeout (4.5s threshold).
    - `offline`: Upstream is unreachable or encounters network error.
    In serverless environments (e.g. Vercel), in-memory cache buffers are ephemeral and container-scoped; fallback timestamps must never be assumed persistent across cold starts. UI consumers must render neutral pulse skeletons (`animate-pulse`) for cold-load initial states, empty-state containers for zero-buffer logs, and relative elapsed time labels (`hace 45s`, `2m ago`) with full i18n support.
17. **Two-Phase Branch Audit & Harmonization Workflow:** When tasked with evaluating or integrating feature branches into a newer target branch with updated visual designs:
    - Phase 1 (Audit & Verification): Maintain strict read-only execution (zero code edits). Use `git merge-tree` to simulate merges and detect overlapping JSX conflicts early. Present an implementation plan artifact for review.
    - Phase 2 (Harmonization): Perform integration on a temporary branch. Preserve modern design spacing tokens, padding utilities, and relaxed polling intervals from the newer design. Ensure modular components remain strictly under 200 LOC, static type checks (`npx tsc --noEmit`) pass with zero errors, and full Vitest suites pass before fast-forwarding to `main`.
18. **Signature Brand Motion & Coordinate Normalization Invariant:** The signature brand morphing animation (scaling the monumental hero name down into the docked navbar logo on scroll) must NEVER be disabled, stripped, or replaced with static text on mobile or tablet viewports:
    - **Screen-Wide Monumental Typography:** The hero title must dynamically adapt to span the full available width of the screen (`heroWidth = Math.max(navWidth, window.innerWidth - navLeft * 2)`). The SVG letterforms must occupy 99%+ of the viewBox width (`viewBox="0 0 1040 105"`, `text-[100px]`, `y="86"`, `tracking-[-0.035em]`, `preserveAspectRatio="xMinYMid meet"`), strictly locked to the left margin (`curLeft = navLeft`, zero horizontal drift).
    - **Dedicated Vertical Clearance:** To prevent monumental text from ever overlapping or crowding the executive portrait, `#hero-name-target` must provide responsive vertical clearance (`h-16 sm:h-24 md:h-32 lg:h-40 xl:h-48 mb-8 sm:mb-12 lg:mb-16`).
    - **Coordinate Decoupling & Prompt Ease-Out:** The interpolation algorithm must decouple live scroll from the anchor's document position (`heroDocTop = heroTargetRect.top + scrollY + ...`) before calculating viewport offsets. It must employ responsive quadratic ease-out (`ease = p * (2 - p)`) and calibrated scroll thresholds (`~180px–260px`) to immediately shrink and lift the title toward `dockedTop` upon scroll onset, avoiding sluggish lag.
    - **Ergonomic Back-to-Top Interaction:** When fully docked (`p >= 0.999`), the brand button activates `data-back-to-top-active="true"` and routes clicks through Lenis smooth scroll to `#top` using quartic easing (`(t) => t < 0.5 ? 8 * t * t * t * t : 1 - Math.pow(-2 * t + 2, 4) / 2`).
19. **WebKit & Apple Mobile Performance Hygiene:** To prevent viewport freezes, dropped frames, and broken layout constraints on iOS and iPadOS Safari:
    - Never set `overflow-x: hidden` on `html` or `body` tags; WebKit computes `overflow-y` to `auto`, breaking `position: sticky` on descendant containers.
    - Never apply `content-visibility: auto` to containers hosting scroll-linked animations (`useScroll`), as WebKit recalculates document scroll height dynamically, inducing violent scroll rubber-banding.
    - Preserve native 120Hz ProMotion touch momentum in smooth scroll providers (Lenis configured with `syncTouch: false` and without artificial touch multipliers).
    - Omit or hide GPU-intensive full-viewport SVG filters (`feTurbulence`) and per-character Gaussian blur filters on mobile/touch screens (`< 768px`) to prevent WebKit GPUProcess throttling and character clipping.
20. **Career Timeline Horizontal Scrollytelling & Focal Center-Marker Invariant:** The career timeline section (`#experience`) exclusively employs a pinned, scroll-driven horizontal timeline stage (`sticky top-0 h-[100dvh]` inside `h-[280vh]`):
    - **Unified Solid Blue Canvas & Window-Wide Connecting Line:** The section spans full viewport width in solid button blue (`#21426E` light / `#162C4E` dark) with a single continuous horizontal line spanning the entire width of the window from edge to edge.
    - **Left-to-Right Chronological Sequence:** Milestones are sorted strictly from earliest to newest (past to present: `2018` -> `2025`), ensuring natural chronological progression from left to right as the user scrolls down.
    - **Right-Offset Start for Earliest Milestone:** The initial milestone (`2018`) begins offset to the right of the center focus marker (`+240px`, `paddingLeft: calc(50vw + 85px)`), smoothly entering the center spotlight upon scroll onset.
    - **Illuminated Center Focus Reticle:** A glowing focal target ring (`#93C5FD`) and vertical crosshairs anchor the center of the viewport (`left: 50%`) directly over the horizontal connecting line.
    - **Dynamic Contrast & Leftward Opacity Drop:**
      - At the center marker, the active milestone reaches 100% full contrast (`opacity: 1.0`, `scale: 1.06`, pure white typography).
      - Advancing to the left of the center marker, its opacity smoothly drops down to `0.25` and scale reduces to `0.92`, fading into past context ("volviendoce opaco").
      - Incoming milestones from the right wait at subdued opacity (`0.45`).
    - **Absolute Scrollbar Suppression:** Horizontal browser scrollbars are strictly suppressed across all browsers and operating systems using `.no-scrollbar` (`display: none !important; width: 0 !important;` for `::-webkit-scrollbar` and `scrollbar-width: none !important`) and `overflow-hidden` on the pinned stage.
    - **Minimalist Typography over Clunky Badges:** Avoid bulky circular plate badges or nested dropdown accordions. Milestones display monumental start year numerals (`2018`, `2021`, etc.) above the line, glowing nodes on the line, and company, role in italics, and concise descriptions anchored below.
    - **Header Cleanliness:** Prevent redundant eyebrow labels; display only the systematic architectural index tag (`[03]`) above the main heading.
21. **WAAPI-Safe Reactive Scroll Interpolation Invariant:** When interpolating scroll-linked styles (`opacity`, `scale`, translations) based on dynamic item positions or offsets:
    - NEVER pass array-based keyframe offsets (e.g. `[centerProgress - delta, centerProgress, centerProgress + delta]`) where boundary values can evaluate outside the normalized `[0, 1]` range (e.g. negative offsets when `centerProgress < delta` or offsets `> 1.0`).
    - In modern Next.js / Turbopack with Framer Motion, out-of-bound or non-monotonic keyframe arrays cause the browser's Web Animations API (WAAPI) to fail with `TypeError: Failed to execute 'animate' on 'Element': Offsets must be monotonically non-decreasing`.
    - ALWAYS implement dynamic proximity interpolations using functional transform curves `useTransform(scrollYProgress, (s) => ...)` to compute smooth, continuous CSS values safely without generating WAAPI keyframe arrays.
22. **Contact Section In-Page AI Terminal & Direct Webmail Invariant:**
    - **Embedded AI Terminal over Form Wizards:** The `#contact` section strictly replaces static multi-step questionnaire forms with an embedded interactive AI assistant terminal (`ContactTerminal`). Visitors must be able to ask questions directly or trigger high-intent actions via quick chips without navigating away or filling form wizards.
    - **Minimal Header & Heroic Terminal Centerpiece:** Strictly eliminate redundant section headings (e.g. `SectionLabel` with "Hablemos de Crecimiento & Automatización" or long explanatory descriptions) and operating manifesto callout quote boxes above the terminal stage. The section opens exclusively with a subtle, single-line bracketed architectural index tag (`[06]` with `eyebrow`), establishing the monumental **HABLEMOS** typography and **ContactTerminal** as the clean, direct focal centerpiece of `#contact`.
    - **Monumental Flanking Typography:** The terminal card must be flanked on desktop viewports by monumental, architectural typography in subdued accent slate blue (`HABLE` on the left, `MOS` on the right in Spanish; `LET'S` on the left, `TALK` on the right in English). On mobile viewports, the words stack cleanly above the terminal.
    - **Direct Webmail Compose Link:** Never rely exclusively on raw `mailto:` protocols for email conversions. Always pair or route email CTAs to direct Gmail compose (`https://mail.google.com/mail/?view=cm&fs=1&to=rop.martel@gmail.com`) across both Clippo conversational prompts and the contact action bar.
    - **Dual Interactive Email Pill:** The primary email display must render as a dual-action component: an external link button that opens Gmail compose in a new tab, paired seamlessly with an integrated 1-click clipboard copy button.
23. **Conversational AI Agent (Clippo) & AAA Persona Alignment Invariant:**
    - **Identity & Role Grounding:** Clippo must always ground Rodrigo as a marketing professional with a casual, authentic, and approachable voice who bridges commercial strategy with practical technology, data analytics, and automated workflows. Never describe Rodrigo as a software engineer, enterprise developer, or ML researcher. Exaggerated engineering titles and corporate buzzwords are strictly forbidden. Always position technical projects, scripts, and codebases as pragmatic enablers for better marketing, operational workflows, and business growth (adhering strictly to Rule 4).
    - **Strict Per-Turn Language Mirroring:** Clippo must dynamically detect and mirror the language of the visitor's latest query (Spanish -> Spanish, English -> English) on every turn. Default browser headers or environment flags must never override the visitor's explicit message language.
    - **Conversion Action Pairing:** When visitors inquire about getting in touch, scheduling a call, or collaborating, Clippo must prioritize offering direct instant messaging (**WhatsApp**) and conversational booking (**Schedule Call** / `open-clippo-schedule`) before offering asynchronous inbox form delivery (`send_contact_email`), adhering to Rule 9.
    - **Credential Integrity:** Never mention aggregate credential totals (e.g. "103 certificates") in system prompts or responses (Rule 6). Frame credentials qualitatively by accredited issuer authority (Google, Microsoft, HubSpot, CertiProf) and practical marketing/analytics application.
    - **Bilingual Error Fallbacks:** Rate-limit (429) and network error fallback messages must dynamically adapt to the language of the visitor's latest message rather than returning hardcoded single-language strings.
    - **Modular File Limits:** Keep all chatbot route handlers (`app/api/chat/route.ts`), prompt builders (`src/lib/gemini.ts`), tool definitions (`src/lib/chatbot-tools.ts`), and UI subcomponents strictly under 200 lines of code (Rule 3).
24. **Case Studies & "Antes vs Después" Architectural Invariant:**
    - **Synthetic Diagram Anti-Pattern:** Strictly forbid artificial toy diagrams, fake telemetry dashboards with truncated text labels, or simulated progress meters. Synthetic mockups confuse visitors, duplicate numbers, and detract from practitioner credibility.
    - **Problem ➔ Solution ➔ Real Impact Framework:** Case studies in `[05] Case Studies` (`#case-studies`) must exclusively employ the clear, authentic **Antes vs Después** editorial architecture:
      - **`Antes (El Problema)`:** Concrete business friction, bottleneck, or inefficiency explained in plain, transparent language.
      - **`Después (La Solución)`:** The practical technical implementation paired with concise numbered execution steps (`01`, `02`, `03`).
      - **`Impacto Medible`:** A single primary monospace metric anchor (`+40%`, `<1.5s`), supporting secondary metrics, and real tool chips. Never repeat the same KPI number multiple times across the card.
    - **100% SEO & AEO Crawlability Invariant:** Case study data must never be hidden exclusively behind client-side state without semantic fallback. Always maintain structured, semantic Schema.org (`itemType="https://schema.org/CreativeWork"`) pre-rendering in the initial DOM so web crawlers and AI answer engines (Perplexity, ChatGPT Search, Gemini) have unhindered access to all titles, challenges, solutions, and tool stacks.
    - **Tactile Segmented Navigation & Modular Limits:** Employ clean, responsive category button pills with spring-animated sliding indicators (`layoutId`), keeping file sizes strictly under 200 LOC per Rule 3.
25. **WAAPI-Safe SVG Vector Animation & GPU Transform Invariant:**
    - **No Path String Morphing:** Never mutate SVG `d` path attributes via Framer Motion keyframe arrays (e.g. `animate={{ d: [pathA, pathB] }}`). In modern Web Animations API (WAAPI), animating SVG `d` commands without dedicated morphing plugins evaluates `d` to `undefined` on initial evaluation and throws fatal browser console exceptions (`Error: <path> attribute d: Expected moveto path command ('M' or 'm'), "undefined"`), degrading Lighthouse Best Practices and accessibility.
    - **GPU Transform Acceleration:** Always animate SVG vector features (such as Clippo eyebrows, pupils, and wire loops) using hardware-accelerated CSS transforms (`y`, `scaleY`, `rotate`, `opacity`) or static `d` attributes with dedicated CSS transitions, ensuring 60 FPS performance and zero console errors.
26. **Initial Media Payload & Viewport Intersection Gating Invariant:**
    - **Deferred Stream Mounting:** To prevent mobile bandwidth waste and avoid ballooning initial network transfer (e.g. 8–10 MB payloads throttling Core Web Vitals LCP and FCP), `<video>` elements located in below-the-fold scroll sections (such as project accordions) must NEVER use unconditional `autoPlay` or `preload="auto"` upon initial mount.
    - **Intersection Gating:** Media elements must gate their `src`, `preload`, and `autoPlay` on true viewport intersection (`isInViewport` via `IntersectionObserver`). Above-the-fold renders a lightweight static poster image until the user scrolls into proximity, whereupon the observer attaches the `src` and activates video playback seamlessly.
27. **Singular `<h1>` Hierarchy, Form A11y & GEO (`llms.txt`) Invariant:**
    - **Singular H1:** Ensure exactly one `<h1>` exists across the entire document (the Hero centerpiece brand anchor). Downstream major section titles must render as `<h2>` via `<ScatterTitle as="h2" />`.
    - **Accessible Form Controls:** All form submit buttons (e.g. `ContactTerminal`, `ChatInput`) must have explicit bilingual `aria-label` attributes (`title` alone is insufficient for screen readers and Lighthouse `button-name` audits).
    - **Generative Engine Optimization (GEO):** Maintain synchronized `/public/llms.txt` and `/public/llms-full.txt` digests, paired with explicit bot allowances (`GPTBot`, `ClaudeBot`, `PerplexityBot`, `Google-Extended`) in `app/robots.ts` to ensure unhindered AI retrieval readiness.

